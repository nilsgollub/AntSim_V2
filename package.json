{
  "name": "antsim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:soak": "vitest run --config vitest.soak.config.ts",
    "test:all": "npm run test && npm run test:soak",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "^7.2.1",
    "terser": "^5.44.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "pixi-filters": "^6.1.5",
    "pixi.js": "^8.18.1"
  }
}
